name: Deploy to EC2

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: SSH Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            cd /home/ubuntu/yourhousehelper-server

            git pull origin main

            ./gradlew build -x test

            cp build/libs/*.jar /home/ubuntu/yourhousehelper-0.0.1-SNAPSHOT.jar

            sudo systemctl restart yourhousehelper
